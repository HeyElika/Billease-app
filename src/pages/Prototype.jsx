export default function Prototype() {
  return (
    <div
      style={{
        padding: 'var(--space-600)',
        fontFamily: 'var(--font-family)',
      }}
    >
      <p
        style={{
          fontSize: 'var(--text-md)',
          color: 'var(--text-subtle)',
          margin: 0,
        }}
      >
        Prototype Builder — coming soon.
      </p>
    </div>
  )
}
